diff --git a/core/iwasm/include/wasm_c_api.h b/core/iwasm/include/wasm_c_api.h
index 241a0ee..edb0b8b 100644
--- a/core/iwasm/include/wasm_c_api.h
+++ b/core/iwasm/include/wasm_c_api.h
@@ -534,6 +534,9 @@ typedef struct LoadArgs {
     bool clone_wasm_binary;
     /* This option is only used by the AOT/wasm loader (see wasm_export.h) */
     bool wasm_binary_freeable;
+    /* This option is only used by the Wasm interpreter loader. If true, the
+       retained input buffer cannot be modified in place. */
+    bool wasm_binary_readonly;
     /* false by default, if true, don't resolve the symbols yet. The
        wasm_runtime_load_ex has to be followed by a wasm_runtime_resolve_symbols
        call */
diff --git a/core/iwasm/include/wasm_export.h b/core/iwasm/include/wasm_export.h
index 81efb8f..4f49bf1 100644
--- a/core/iwasm/include/wasm_export.h
+++ b/core/iwasm/include/wasm_export.h
@@ -271,6 +271,11 @@ typedef struct LoadArgs {
     loading. */
     bool wasm_binary_freeable;
 
+    /* False by default. If true, the retained Wasm input buffer cannot be
+       modified in place, so the interpreter loader copies const strings while
+       continuing to reference bytecode and data segments from that buffer. */
+    bool wasm_binary_readonly;
+
     /* false by default, if true, don't resolve the symbols yet. The
        wasm_runtime_load_ex has to be followed by a wasm_runtime_resolve_symbols
        call */
diff --git a/core/iwasm/interpreter/wasm.h b/core/iwasm/interpreter/wasm.h
index 0dd7395..4a21dc0 100644
--- a/core/iwasm/interpreter/wasm.h
+++ b/core/iwasm/interpreter/wasm.h
@@ -681,6 +681,14 @@ typedef struct WASMImport {
     } u;
 } WASMImport;
 
+#if WASM_ENABLE_FAST_INTERP == 0
+typedef struct WASMBytecodePatch {
+    struct WASMBytecodePatch *next;
+    const uint8 *opcode_addr;
+    uint8 opcode;
+} WASMBytecodePatch;
+#endif
+
 struct WASMFunction {
 #if WASM_ENABLE_CUSTOM_NAME_SECTION != 0
     char *field_name;
@@ -713,6 +721,11 @@ struct WASMFunction {
     uint8 *code_compiled;
     uint8 *consts;
     uint32 const_cell_num;
+#else
+    /* Classic interpretation normally rewrites a few opcodes in-place after
+       validation.  Keep those one-byte rewrites out-of-line when the source
+       Wasm lives in immutable/XIP storage. */
+    WASMBytecodePatch *bytecode_patches;
 #endif
 
 #if WASM_ENABLE_GC != 0
@@ -981,6 +994,9 @@ struct WASMModule {
     /* Whether there is possible memory grow, e.g. memory.grow opcode */
     bool possible_memory_grow;
 
+    /* The source Wasm buffer cannot be modified (for example data-XIP flash). */
+    bool wasm_binary_readonly;
+
     StringList const_str_list;
 #if WASM_ENABLE_FAST_INTERP == 0
     bh_list br_table_cache_list_head;
diff --git a/core/iwasm/interpreter/wasm_interp_classic.c b/core/iwasm/interpreter/wasm_interp_classic.c
index 9f7e03e..36a5895 100644
--- a/core/iwasm/interpreter/wasm_interp_classic.c
+++ b/core/iwasm/interpreter/wasm_interp_classic.c
@@ -1470,10 +1470,35 @@ wasm_interp_call_func_import(WASMModuleInstance *module_inst,
 #endif
 #endif
 
+static inline uint8
+resolve_bytecode_opcode(WASMFunction *func, const uint8 *opcode_addr,
+                        uint8 opcode)
+{
+#if WASM_ENABLE_FAST_INTERP == 0
+    WASMBytecodePatch *patch = func->bytecode_patches;
+
+    while (patch) {
+        if (patch->opcode_addr == opcode_addr)
+            return patch->opcode;
+        patch = patch->next;
+    }
+#else
+    (void)func;
+    (void)opcode_addr;
+#endif
+    return opcode;
+}
+
 #if WASM_ENABLE_LABELS_AS_VALUES != 0
 
 #define HANDLE_OP(opcode) HANDLE_##opcode:
-#define FETCH_OPCODE_AND_DISPATCH() goto *handle_table[*frame_ip++]
+#define FETCH_OPCODE_AND_DISPATCH()                                    \
+    do {                                                               \
+        opcode = resolve_bytecode_opcode(cur_func->u.func, frame_ip,    \
+                                         *frame_ip);                    \
+        frame_ip++;                                                     \
+        goto *handle_table[opcode];                                     \
+    } while (0)
 
 #if WASM_ENABLE_THREAD_MGR != 0 && WASM_ENABLE_DEBUG_INTERP != 0
 #define HANDLE_OP_END()                                                       \
@@ -1673,7 +1698,9 @@ wasm_interp_call_func_bytecode(WASMModuleInstance *module,
 
 #if WASM_ENABLE_LABELS_AS_VALUES == 0
     while (frame_ip < frame_ip_end) {
-        opcode = *frame_ip++;
+        opcode = resolve_bytecode_opcode(cur_func->u.func, frame_ip,
+                                         *frame_ip);
+        frame_ip++;
         switch (opcode) {
 #else
     FETCH_OPCODE_AND_DISPATCH();
@@ -2258,7 +2285,13 @@ wasm_interp_call_func_bytecode(WASMModuleInstance *module,
                 lidx = POP_I32();
                 if (lidx > count)
                     lidx = count;
-                depth = frame_ip[lidx];
+                if (module->module->wasm_binary_readonly) {
+                    for (i = 0; i <= lidx; i++)
+                        read_leb_uint32(frame_ip, frame_ip_end, depth);
+                }
+                else {
+                    depth = frame_ip[lidx];
+                }
                 goto label_pop_csp_n;
             }
 
diff --git a/core/iwasm/interpreter/wasm_loader.c b/core/iwasm/interpreter/wasm_loader.c
index d3f4720..09b9ad3 100644
--- a/core/iwasm/interpreter/wasm_loader.c
+++ b/core/iwasm/interpreter/wasm_loader.c
@@ -6121,7 +6121,8 @@ static void **handle_table;
 static bool
 load_from_sections(WASMModule *module, WASMSection *sections,
                    bool is_load_from_file_buf, bool wasm_binary_freeable,
-                   bool no_resolve, char *error_buf, uint32 error_buf_size)
+                   bool wasm_binary_readonly, bool no_resolve,
+                   char *error_buf, uint32 error_buf_size)
 {
     WASMExport *export;
     WASMSection *section = sections;
@@ -6136,7 +6137,8 @@ load_from_sections(WASMModule *module, WASMSection *sections,
     uint32 aux_heap_base_global_index = (uint32)-1;
     WASMFuncType *func_type;
     uint8 malloc_free_io_type = VALUE_TYPE_I32;
-    bool reuse_const_strings = is_load_from_file_buf && !wasm_binary_freeable;
+    bool reuse_const_strings = is_load_from_file_buf && !wasm_binary_freeable
+                               && !wasm_binary_readonly;
     bool clone_data_seg = is_load_from_file_buf && wasm_binary_freeable;
 #if WASM_ENABLE_BULK_MEMORY != 0
     bool has_datacount_section = false;
@@ -6748,8 +6750,8 @@ wasm_loader_load_from_sections(WASMSection *section_list, char *error_buf,
     if (!module)
         return NULL;
 
-    if (!load_from_sections(module, section_list, false, true, false, error_buf,
-                            error_buf_size)) {
+    if (!load_from_sections(module, section_list, false, true, false, false,
+                            error_buf, error_buf_size)) {
         wasm_loader_unload(module);
         return NULL;
     }
@@ -6893,8 +6895,8 @@ static union {
 
 static bool
 load(const uint8 *buf, uint32 size, WASMModule *module,
-     bool wasm_binary_freeable, bool no_resolve, char *error_buf,
-     uint32 error_buf_size)
+     bool wasm_binary_freeable, bool wasm_binary_readonly, bool no_resolve,
+     char *error_buf, uint32 error_buf_size)
 {
     const uint8 *buf_end = buf + size;
     const uint8 *p = buf, *p_end = buf_end;
@@ -6925,7 +6927,8 @@ load(const uint8 *buf, uint32 size, WASMModule *module,
 
     if (!create_sections(buf, size, &section_list, error_buf, error_buf_size)
         || !load_from_sections(module, section_list, true, wasm_binary_freeable,
-                               no_resolve, error_buf, error_buf_size)) {
+                               wasm_binary_readonly, no_resolve, error_buf,
+                               error_buf_size)) {
         destroy_sections(section_list);
         return false;
     }
@@ -7101,8 +7104,9 @@ wasm_loader_load(uint8 *buf, uint32 size,
     module->load_size = size;
 #endif
 
-    if (!load(buf, size, module, args->wasm_binary_freeable, args->no_resolve,
-              error_buf, error_buf_size)) {
+    if (!load(buf, size, module, args->wasm_binary_freeable,
+              args->wasm_binary_readonly, args->no_resolve, error_buf,
+              error_buf_size)) {
         goto fail;
     }
 
diff --git a/core/iwasm/interpreter/wasm_mini_loader.c b/core/iwasm/interpreter/wasm_mini_loader.c
index 1fbbb8d..a402c66 100644
--- a/core/iwasm/interpreter/wasm_mini_loader.c
+++ b/core/iwasm/interpreter/wasm_mini_loader.c
@@ -202,6 +202,24 @@ loader_malloc(uint64 size, char *error_buf, uint32 error_buf_size)
     return mem;
 }
 
+#if WASM_ENABLE_FAST_INTERP == 0
+static bool
+add_bytecode_patch(WASMFunction *func, const uint8 *opcode_addr, uint8 opcode,
+                   char *error_buf, uint32 error_buf_size)
+{
+    WASMBytecodePatch *patch =
+        loader_malloc(sizeof(*patch), error_buf, error_buf_size);
+
+    if (!patch)
+        return false;
+    patch->opcode_addr = opcode_addr;
+    patch->opcode = opcode;
+    patch->next = func->bytecode_patches;
+    func->bytecode_patches = patch;
+    return true;
+}
+#endif
+
 static void *
 memory_realloc(void *mem_old, uint32 size_old, uint32 size_new, char *error_buf,
                uint32 error_buf_size)
@@ -2807,7 +2825,8 @@ static void **handle_table;
 static bool
 load_from_sections(WASMModule *module, WASMSection *sections,
                    bool is_load_from_file_buf, bool wasm_binary_freeable,
-                   char *error_buf, uint32 error_buf_size)
+                   bool wasm_binary_readonly, char *error_buf,
+                   uint32 error_buf_size)
 {
     WASMExport *export;
     WASMSection *section = sections;
@@ -2822,12 +2841,15 @@ load_from_sections(WASMModule *module, WASMSection *sections,
     uint32 aux_heap_base_global_index = (uint32)-1;
     WASMFuncType *func_type;
     uint8 malloc_free_io_type = VALUE_TYPE_I32;
-    bool reuse_const_strings = is_load_from_file_buf && !wasm_binary_freeable;
+    bool reuse_const_strings = is_load_from_file_buf && !wasm_binary_freeable
+                               && !wasm_binary_readonly;
     bool clone_data_seg = is_load_from_file_buf && wasm_binary_freeable;
 #if WASM_ENABLE_BULK_MEMORY != 0
     bool has_datacount_section = false;
 #endif
 
+    module->wasm_binary_readonly = wasm_binary_readonly;
+
     /* Find code and function sections if have */
     while (section) {
         if (section->section_type == SECTION_TYPE_CODE) {
@@ -3346,7 +3368,7 @@ wasm_loader_load_from_sections(WASMSection *section_list, char *error_buf,
     if (!module)
         return NULL;
 
-    if (!load_from_sections(module, section_list, false, true, error_buf,
+    if (!load_from_sections(module, section_list, false, true, false, error_buf,
                             error_buf_size)) {
         wasm_loader_unload(module);
         return NULL;
@@ -3476,7 +3498,8 @@ static union {
 
 static bool
 load(const uint8 *buf, uint32 size, WASMModule *module,
-     bool wasm_binary_freeable, char *error_buf, uint32 error_buf_size)
+     bool wasm_binary_freeable, bool wasm_binary_readonly, char *error_buf,
+     uint32 error_buf_size)
 {
     const uint8 *buf_end = buf + size;
     const uint8 *p = buf, *p_end = buf_end;
@@ -3502,7 +3525,8 @@ load(const uint8 *buf, uint32 size, WASMModule *module,
 
     if (!create_sections(buf, size, &section_list, error_buf, error_buf_size)
         || !load_from_sections(module, section_list, true, wasm_binary_freeable,
-                               error_buf, error_buf_size)) {
+                               wasm_binary_readonly, error_buf,
+                               error_buf_size)) {
         destroy_sections(section_list);
         return false;
     }
@@ -3530,8 +3554,8 @@ wasm_loader_load(uint8 *buf, uint32 size,
     module->load_size = size;
 #endif
 
-    if (!load(buf, size, module, args->wasm_binary_freeable, error_buf,
-              error_buf_size)) {
+    if (!load(buf, size, module, args->wasm_binary_freeable,
+              args->wasm_binary_readonly, error_buf, error_buf_size)) {
         goto fail;
     }
 
@@ -3606,6 +3630,18 @@ wasm_loader_unload(WASMModule *module)
                     wasm_runtime_free(module->functions[i]->code_compiled);
                 if (module->functions[i]->consts)
                     wasm_runtime_free(module->functions[i]->consts);
+#else
+                {
+                    WASMBytecodePatch *patch =
+                        module->functions[i]->bytecode_patches;
+
+                    while (patch) {
+                        WASMBytecodePatch *next = patch->next;
+
+                        wasm_runtime_free(patch);
+                        patch = next;
+                    }
+                }
 #endif
 #if WASM_ENABLE_FAST_JIT != 0
                 if (module->functions[i]->fast_jit_jitted_code) {
@@ -6422,6 +6458,19 @@ wasm_loader_prepare_bytecode(WASMModule *module, WASMFunction *func,
     table_elem_idx_type = VALUE_TYPE_I32;
 #endif
 
+#if WASM_ENABLE_FAST_INTERP == 0
+#define PATCH_OPCODE(opcode_addr, replacement)                              \
+    do {                                                                    \
+        if (module->wasm_binary_readonly) {                                 \
+            if (!add_bytecode_patch(func, opcode_addr, replacement,         \
+                                    error_buf, error_buf_size))             \
+                goto fail;                                                  \
+        }                                                                   \
+        else                                                                \
+            *(opcode_addr) = (replacement);                                 \
+    } while (0)
+#endif
+
     global_count = module->import_global_count + module->global_count;
 
     param_count = func->func_type->param_count;
@@ -6598,7 +6647,8 @@ re_scan:
                      * to new extended opcode so that interpreter can resolve
                      * the block quickly.
                      */
-                    *p_org = EXT_OP_BLOCK + (opcode - WASM_OP_BLOCK);
+                    PATCH_OPCODE(
+                        p_org, EXT_OP_BLOCK + (opcode - WASM_OP_BLOCK));
 #endif
                 }
 
@@ -6928,36 +6978,40 @@ re_scan:
                         goto fail;
 
 #if WASM_ENABLE_FAST_INTERP == 0
-                    depth = (uint32)(loader_ctx->frame_csp - 1 - frame_csp_tmp);
-                    if (br_table_cache) {
-                        br_table_cache->br_depths[i] = depth;
-                    }
-                    else {
-                        if (depth > 255) {
-                            /* The depth cannot be stored in one byte,
-                               create br_table cache to store each depth */
-                            if (!(br_table_cache = loader_malloc(
-                                      offsetof(BrTableCache, br_depths)
-                                          + sizeof(uint32)
-                                                * (uint64)(count + 1),
-                                      error_buf, error_buf_size))) {
-                                goto fail;
-                            }
-                            *p_opcode = EXT_OP_BR_TABLE_CACHE;
-                            br_table_cache->br_table_op_addr = p_opcode;
-                            br_table_cache->br_count = count;
-                            /* Copy previous depths which are one byte */
-                            for (j = 0; j < i; j++) {
-                                br_table_cache->br_depths[j] = p_depth_begin[j];
-                            }
+                    if (!module->wasm_binary_readonly) {
+                        depth = (uint32)(loader_ctx->frame_csp - 1
+                                         - frame_csp_tmp);
+                        if (br_table_cache) {
                             br_table_cache->br_depths[i] = depth;
-                            bh_list_insert(module->br_table_cache_list,
-                                           br_table_cache);
                         }
                         else {
-                            /* The depth can be stored in one byte, use the
-                               byte of the leb to store it */
-                            *p_depth++ = (uint8)depth;
+                            if (depth > 255) {
+                                /* The depth cannot be stored in one byte,
+                                   create br_table cache to store each depth */
+                                if (!(br_table_cache = loader_malloc(
+                                          offsetof(BrTableCache, br_depths)
+                                              + sizeof(uint32)
+                                                    * (uint64)(count + 1),
+                                          error_buf, error_buf_size))) {
+                                    goto fail;
+                                }
+                                *p_opcode = EXT_OP_BR_TABLE_CACHE;
+                                br_table_cache->br_table_op_addr = p_opcode;
+                                br_table_cache->br_count = count;
+                                /* Copy previous depths which are one byte */
+                                for (j = 0; j < i; j++) {
+                                    br_table_cache->br_depths[j] =
+                                        p_depth_begin[j];
+                                }
+                                br_table_cache->br_depths[i] = depth;
+                                bh_list_insert(module->br_table_cache_list,
+                                               br_table_cache);
+                            }
+                            else {
+                                /* The depth can be stored in one byte, use the
+                                   byte of the leb to store it */
+                                *p_depth++ = (uint8)depth;
+                            }
                         }
                     }
 #endif
@@ -6965,10 +7019,12 @@ re_scan:
 
 #if WASM_ENABLE_FAST_INTERP == 0
                 /* Set the tailing bytes to nop */
-                if (br_table_cache)
-                    p_depth = p_depth_begin;
-                while (p_depth < p)
-                    *p_depth++ = WASM_OP_NOP;
+                if (!module->wasm_binary_readonly) {
+                    if (br_table_cache)
+                        p_depth = p_depth_begin;
+                    while (p_depth < p)
+                        *p_depth++ = WASM_OP_NOP;
+                }
 #endif
 
                 RESET_STACK();
@@ -7206,7 +7262,7 @@ re_scan:
                         loader_ctx->frame_ref -= 2;
                         loader_ctx->stack_cell_num -= 2;
 #if WASM_ENABLE_FAST_INTERP == 0
-                        *(p - 1) = WASM_OP_DROP_64;
+                        PATCH_OPCODE(p - 1, WASM_OP_DROP_64);
 #endif
 #if WASM_ENABLE_FAST_INTERP != 0
                         skip_label();
@@ -7256,7 +7312,7 @@ re_scan:
                         case REF_I64_2:
                         case REF_F64_2:
 #if WASM_ENABLE_FAST_INTERP == 0
-                            *(p - 1) = WASM_OP_SELECT_64;
+                            PATCH_OPCODE(p - 1, WASM_OP_SELECT_64);
 #endif
 #if WASM_ENABLE_FAST_INTERP != 0
                             if (loader_ctx->p_code_compiled) {
@@ -7578,7 +7634,7 @@ re_scan:
 #else
 #if (WASM_ENABLE_WAMR_COMPILER == 0) && (WASM_ENABLE_JIT == 0) \
     && (WASM_ENABLE_FAST_JIT == 0)
-                if (local_offset < 0x80) {
+                if (!module->wasm_binary_readonly && local_offset < 0x80) {
                     *p_org++ = EXT_OP_GET_LOCAL_FAST;
                     if (is_32bit_type(local_type))
                         *p_org++ = (uint8)local_offset;
@@ -7638,7 +7694,7 @@ re_scan:
 #else
 #if (WASM_ENABLE_WAMR_COMPILER == 0) && (WASM_ENABLE_JIT == 0) \
     && (WASM_ENABLE_FAST_JIT == 0)
-                if (local_offset < 0x80) {
+                if (!module->wasm_binary_readonly && local_offset < 0x80) {
                     *p_org++ = EXT_OP_SET_LOCAL_FAST;
                     if (is_32bit_type(local_type))
                         *p_org++ = (uint8)local_offset;
@@ -7696,7 +7752,7 @@ re_scan:
 #else
 #if (WASM_ENABLE_WAMR_COMPILER == 0) && (WASM_ENABLE_JIT == 0) \
     && (WASM_ENABLE_FAST_JIT == 0)
-                if (local_offset < 0x80) {
+                if (!module->wasm_binary_readonly && local_offset < 0x80) {
                     *p_org++ = EXT_OP_TEE_LOCAL_FAST;
                     if (is_32bit_type(local_type))
                         *p_org++ = (uint8)local_offset;
@@ -7729,7 +7785,7 @@ re_scan:
 #if WASM_ENABLE_FAST_INTERP == 0
                 if (global_type == VALUE_TYPE_I64
                     || global_type == VALUE_TYPE_F64) {
-                    *p_org = WASM_OP_GET_GLOBAL_64;
+                    PATCH_OPCODE(p_org, WASM_OP_GET_GLOBAL_64);
                 }
 #else  /* else of WASM_ENABLE_FAST_INTERP */
                 if (is_64bit_type(global_type)) {
@@ -7769,11 +7825,11 @@ re_scan:
 
 #if WASM_ENABLE_FAST_INTERP == 0
                 if (is_64bit_type(global_type)) {
-                    *p_org = WASM_OP_SET_GLOBAL_64;
+                    PATCH_OPCODE(p_org, WASM_OP_SET_GLOBAL_64);
                 }
                 else if (module->aux_stack_size > 0
                          && global_idx == module->aux_stack_top_global_index) {
-                    *p_org = WASM_OP_SET_GLOBAL_AUX_STACK;
+                    PATCH_OPCODE(p_org, WASM_OP_SET_GLOBAL_AUX_STACK);
 #if WASM_ENABLE_JIT != 0 || WASM_ENABLE_WAMR_COMPILER != 0
                     func->has_op_set_global_aux_stack = true;
 #endif
@@ -8716,3 +8772,7 @@ fail:
 #endif
     return return_value;
 }
+
+#if WASM_ENABLE_FAST_INTERP == 0
+#undef PATCH_OPCODE
+#endif
